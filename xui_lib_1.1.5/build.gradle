apply plugin: 'com.android.library'
//apply plugin: 'img-optimizer'
apply plugin: 'com.github.dcendents.android-maven'
//group='com.github.repository-zhangqingyou'
apply plugin: 'com.novoda.bintray-release'
android {
    compileSdkVersion build_versions.target_sdk

    defaultConfig {
        minSdkVersion build_versions.min_sdk
        targetSdkVersion build_versions.target_sdk
    }

    lintOptions {
        abortOnError false
    }
}

dependencies {
    api "androidx.appcompat:appcompat:$versions.androidx"//对应appcompat-v7
    api "androidx.constraintlayout:constraintlayout:$versions.constraintlayout"
    api "com.google.android.material:material:$versions.androidx"//对应support:design
    api "androidx.recyclerview:recyclerview:$versions.androidx"
    //glide图片加载库
    api("com.github.bumptech.glide:glide:$versions.glide") {
        exclude group: 'com.android.support'
    }
    annotationProcessor "com.github.bumptech.glide:compiler:$versions.glide"
    api 'io.github.inflationx:calligraphy3:3.1.1'
    api 'io.github.inflationx:viewpump:2.0.3'
}
publish {
    bintrayUser = 'zhangqingyou'//用户账号
    bintrayKey = '96d51763bea57597abf85991eff2432c18d5faad'//apikey
    userOrg = 'zhangqingyou' // bintray 用户名
    repoName = 'Android' // bintray 仓库名
    groupId = 'com.bintray.zhangqingyou' // 依赖的路径
    artifactId = 's_xui_lib_1.1.5' // 依赖的名称
    publishVersion = "$versions.publish_version" // 依赖的版本号
    desc = 'XUI' // 描述
    website = 'https://gitee.com/zqycl/' // 项目网站，可填写 github 地址
    dryRun = false // true-仅运行  false-运行+上传
    licences = ['Apache-2.0']
}
